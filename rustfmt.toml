# Formatting standard for the clew workspace. Apply with `cargo fmt --all`.
# Pinned so formatting stays stable across rustfmt versions and machines.
edition = "2024"
max_width = 100
newline_style = "Unix"
reorder_imports = true
reorder_modules = true
